import React from "react";
import ReactDOM from "react-dom/client";
import RequestTabApp from "./RequestTabApp";
import "../styles/globals.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RequestTabApp />
  </React.StrictMode>
);
